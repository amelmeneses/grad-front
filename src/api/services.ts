// import api from "./api";
// import { Service } from "../interfaces/Service";

// export const getServices = async () => (await api.get("/services")).data;
// export const createService = async (service: Service) => (await api.post("/services", service)).data;
// export const updateService = async (id: number, service: Service) => (await api.put(`/services/${id}`, service)).data;
// export const deleteService = async (id: number) => (await api.delete(`/services/${id}`)).data;
// export const getServicesByCourt = async (courtId: number) => (await api.get(`/services/court/${courtId}`)).data;
// // 